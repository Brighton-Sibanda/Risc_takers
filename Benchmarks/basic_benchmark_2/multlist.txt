    lw x14, 0(x10)  # Load word from memory address in x10 to x14
multlstloop:
    bge x14, x11, done
    lw x15, 0(x10)  # Load word from memory address in x10 to x15
    mul x15, x15, x12
    sw x15, 0(x13)  # Store word in x15 to memory address in x13
    addi x10, x10, 4
    addi x13, x13, 4
    addi x14, x14, 1
    j multlstloop
done:
